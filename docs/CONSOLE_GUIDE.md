# 콘솔 등록 가이드 — 나만의고물상

> 앱인토스 콘솔에 입력할 값 모음. 코드/빌드는 완료(`junkyard.ait`), 아래만 채우면 출시.
> 최신 deploymentId: **`019e9813-4cb6-7c45-8b86-fcc29f66fba6`**

## 1. 기본 정보
| 항목 | 값 |
|------|-----|
| 한국어 앱 이름 | 나만의고물상 |
| 영어 앱 이름 | Junkyard |
| appName | junkyard |
| 사용 연령 | 만 19세 이상 |
| 부제 (≤20자) | **주운 고물이 토스 포인트로** |
| 고객센터 이메일 | ksw03090@gmail.com |

**상세 설명**
> 손수레에 고물이 시간마다 자동으로 쌓여요. '추가로 줍기'를 누르면 광고를 보고 고철처럼 비싼 고물을 더 주울 수 있어요. 손수레가 차면 '창고로 옮기기'로 옮기고, 창고에서 '전부 팔기'를 누르면 엽전을 받아요. 모은 엽전은 '토스 포인트로 받기'로 토스 포인트로 바꿀 수 있어요.

## 2. 검색 키워드 (권장)
`고물상` `앱테크` `방치형` `포인트적립` `돈모으기` `고철` `재활용` `용돈` `리워드` `출석체크`

## 3. 카테고리
- **비게임** (게임 등급분류 단계도 비게임으로)

## 4. 프로모션 코드 2개
| 코드 | 미션명(예) | 지급 | 한도 |
|------|-----------|------|------|
| `GOMUL_DAILY` | 나만의고물상 출석 선물 | 1원 고정 | 하루 5회 |
| `GOMUL_REWARD` | 고물 팔고 토스 포인트 받기 | 가변(엽전 130냥=1원) | 일일 15원 / 1인 누적 5,000원 |
- 혜택탭 노출: 둘 다 ON 권장.

## 5. 광고 단위
- **전면(interstitial)** 1개 + **배너** 1개 생성 → 발급된 운영 ID를 코드에 반영.
- 코드 위치: `app/src/constants/adIds.ts` → `IS_PRODUCTION=true` + PROD 배너/전면 ID. (← 콘솔 ID 주시면 제가 채움)

## 6. 비즈월렛
- 보상 예산 충전 (최소 30만원 권장).

## 7. 약관 / 개인정보
- **이용약관 URL: 빙수 등에서 쓰던 공용 약관 URL 그대로 재사용 가능** (앱명 비특정·동일 사업자·동일 구조). 원본 `AppInToss/_문서_앱개발/서비스이용약관.md`.
- 개인정보 처리방침 URL: 공용본 있으면 재사용(약관 제10조가 참조). 없으면 공용으로 작성 필요.
- 코드 placeholder: `copy.ts` `menu.privacyUrlPlaceholder` / `supportEmailPlaceholder` (← 값 주시면 제가 채움).

## 8. 이미지
- **앱 아이콘**: 간판(#21) 정사각 크롭 — 콘솔 권장 px에 맞춤.
- **썸네일**(가로) / **스크린샷**(세로, 실기기 캡처 권장 4컷).

## 9. 업로드
- `.ait` 빌드 업로드 → deploymentId `019e9813...` 배포.
- 업로드 후 QR로 실기기 테스트(손수레·줍기·창고·교환·출석·개근).
